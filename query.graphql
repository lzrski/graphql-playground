query {
  repository(owner: "octocat", name: "Hello-World") {
    issues(last: 10, states: CLOSED) {
      edges {
        node {
          title
          url
          labels(first: 5) {
            edges {
              node {
                name
              }
            }
          }
        }
      }
    }
  }
}
